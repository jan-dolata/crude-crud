{
  "private": true,
  "devDependencies": {},
  "dependencies": {
    "bootstrap-sass": "^3.3.6"
  },
  "version": "1.0.1",
  "scripts": {
    "version": "git add -A dist",
    "postversion": "git push && git push --tags"
  }
}
