{
  "private": true,
  "devDependencies": {},
  "dependencies": {
    "bootstrap-sass": "^3.3.6"
  },
  "version": "1.0.1",
  "scripts": {
    "version": "git add package.json",
    "postversion": "git push && git push --tags"
  }
}
